"""Tradespace exploration layer.

- :mod:`.sweeps`        — parametric sweeps + interactive Jupyter widgets.
- :mod:`.optimizer`     — NSGA-II via pymoo, calling the corrected physics
  evaluator as its default fitness function.
- :mod:`.visualize`     — shared matplotlib helpers for static paper figures.
"""
